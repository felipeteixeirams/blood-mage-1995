import React from 'react';

/**
 * RotateDeviceOverlay (Deprecated):
 * The game no longer displays a blocking full-screen rotate warning.
 * Instead, the game adapts fluidly and uses tryLockLandscape() for
 * non-blocking native orientation locking.
 */
export const RotateDeviceOverlay: React.FC = () => {
  return null;
};
